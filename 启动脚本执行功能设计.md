# 启动脚本执行功能 · 设计文档

> 状态：**设计稿，尚未实现**
> 日期：2026-09-18

---

## 一、背景与目标

后端启动链现在是（`main.go:18-56`）：

```
LoadConfig → InitDB → AutoMigrate → seedTestData → seedLeakTags
```

schema 靠 `AutoMigrate` 自动跟进，但**数据迁移没有地方放**。目前的做法是手写
`database/migrate_*.sql`，然后人工执行：

```bash
mysql -h <host> -u <user> -p <db> < database/migrate_20260915_table_size.sql
```

跑没跑过、跑的是哪一版，全靠人记。

**目标**：把这类一次性脚本收进 `scripts/`，后端启动时自动按序执行，且执行过就不会重跑。

---

## 二、现状：存量脚本不适合自动执行

`database/` 下现有的迁移脚本是**给人工操作设计的**，看写法就知道：

| 特征 | 位置 | 自动执行会怎样 |
|---|---|---|
| `SET @confirm = 0` 人工确认开关 | `migrate_20260917_purge_stale_insights.sql` | 自动跑时是全新会话，`@confirm` 恒为 0，**整个脚本静默变成空操作** |
| 校验用 `SELECT`（期望人看结果） | 两个脚本末尾 | 结果被丢弃，没人看 |
| 注释掉的收尾步骤 `-- DROP TABLE review_insights_stale_20260917;` | 同上 | 备份表永远留着 |
| 注释掉的回滚段 | 同上 | — |
| 文件头写着 `mysql ... < database/xxx.sql` | 两个脚本 | 与自动执行的语义冲突 |

**结论：新机制只扫 `scripts/`，`database/` 保持原样、继续人工执行。**
不要把存量脚本挪进 `scripts/`，否则要么静默空转，要么跳过人工确认直接删数据。

---

## 三、目录结构

```
scripts/
├── embed.go          //go:embed sql —— 把 SQL 编进二进制
├── runner.go         执行器（扫描 / 比对 / 加锁 / 执行 / 记账）
├── runner_test.go    无 DB 的纯函数测试
└── sql/
    ├── README.md     命名与书写约定（兼作 embed 模式的占位，见下）
    └── 20260918_1530_xxx.sql

models/script_run.go  追踪表模型（放 models/ 符合现有约定）
```

### 为什么用 `go:embed`

CI（`.github/workflows/go.yml`）只做：

```
go build -o call_back ./main.go   →   scp 单个二进制   →   systemctl restart call_back
```

**不传任何目录**。如果运行时读磁盘，生产环境会扫不到脚本、**静默什么都不做** —— 这是最难排查的一类故障。

### 为什么 `sql/` 下必须有 README.md

`//go:embed sql` 匹配不到任何文件会**编译失败**，所以该目录不能为空。
放一个 README（执行器按 `.sql` 后缀过滤，会自动跳过它），顺便把书写约定放在脚本目录里，
写脚本的人一定看得到。

### 命名约定

`YYYYMMDD_HHMM_描述.sql`，**按文件名字典序执行**。

带时分是为了同一天有多个脚本时顺序是确定的，而不是靠描述词的字母序碰巧排对。

---

## 四、追踪表

```go
// models/script_run.go
type ScriptRun struct {
    ID         uint      `json:"id" gorm:"primaryKey"`
    Name       string    `json:"name" gorm:"size:255;uniqueIndex;not null;comment:脚本文件名"`
    Checksum   string    `json:"checksum" gorm:"size:64;not null;comment:内容sha256，用于识别已执行脚本被改动"`
    DurationMs int64     `json:"durationMs" gorm:"comment:执行耗时毫秒"`
    ExecutedAt time.Time `json:"executedAt" gorm:"comment:执行完成时间"`
}

func (ScriptRun) TableName() string { return "script_runs" }
```

对齐现有 model 风格（参考 `models/transaction.go:22-40`）：

- `TableName()` 显式返回复数 snake_case，不依赖 GORM 默认复数化
- 每列带中文 `comment:`
- 不加 `gorm.DeletedAt` —— 这是 append-only 的账本，不删

`Name` 上的 `uniqueIndex` 不只是查询用，它是**并发安全的第二道闸**（见第六节）。

该 model 加进 `main.go:33` 的 `AutoMigrate` 列表 —— 沿用项目"只有 AutoMigrate 一个
schema 机制"的现状，不引入第二套。

---

## 五、启动接入点

```go
// main.go，放在 seedLeakTags() 之后、gin.Default() 之前
if err := scripts.Run(config.DB); err != nil {
    log.Fatal("Failed to run startup scripts:", err)
}
```

**顺序理由**：

- `AutoMigrate` 必须在最前。`migrate_20260915_table_size.sql` 自己的注释就写明
  "先重启后端让 AutoMigrate 加列，再执行本脚本"，这是既有约定。
- 放在两个 seeder 之后：脚本可以依赖种子数据已就位；同时脚本不会让
  `seedTestData` 的 `userCount == 0` 判断误跳过。

失败处理与现有三处 `log.Fatal`（`main.go:21/29/49`）保持一致。

---

## 六、执行流程

```
Run(db):
  1. mode = config.ScriptRunMode
     off → 打一行日志返回

  2. 收集脚本
     - 默认从 embed FS 读 sql/*.sql
     - 若 config.ScriptsDir 非空 → 改读该磁盘目录（开发期免编译）
     - 过滤 .sql 后缀，按 Name 字典序排序
     - 算 sha256 作 checksum

  3. applied = SELECT name, checksum FROM script_runs   （map[name]checksum）
     pending = 不在 applied 里的
     drifted = 在 applied 但 checksum 不等的

  4. drifted 非空 → 返回错误，阻断启动（见「checksum 漂移」）

  5. mode == report → 打印 pending 清单（名字 / 字节数 / checksum 前 8 位），返回

  6. mode == apply:
     a. 在迁移连接上 SELECT GET_LOCK('call_go:scripts', 10)
        拿不到 → 报错，说明有另一个实例正在跑
     b. 拿到锁之后【重新查一次 applied】← 关键，见「并发安全」
     c. 逐个执行 pending：成功 → INSERT script_runs；失败 → 返回错误
     d. RELEASE_LOCK
     e. 打印汇总 n executed / m skipped
```

### 6.1 多语句怎么执行

`config/database.go:44` 的 DSN 没有 `multiStatements`，而 go-sql-driver 默认是 `false`，
**一个含多条 `;` 语句的文件直接 `Exec` 会报错**。两条路：

| 方案 | 评价 |
|---|---|
| 在 Go 里自己切分语句 | 约 80 行，要正确处理字符串字面量里的 `;`、`--` 注释、`` ` `` 引号。写错就是静默跑错语句 |
| DSN 加 `multiStatements=true`，整份文件发过去 | 注释由 MySQL 自己解析 —— **现有脚本全是重注释的**，这点很关键 |

**选后者**，但**只给执行器单独开一条连接，不加在应用主连接上** ——
开着它等于放大任何一处 SQL 拼接的影响面。

为此 `config/database.go` 做个小重构（当前两个 DSN 字符串是各自手写的）：

```go
// 抽出单一路径
func buildDSN(dbName string, multiStatements bool) string

// 仅供 scripts 包使用
func DSNWithMultiStatements() string
```

执行器自己 `sql.Open` 这条连接，并设 `SetMaxOpenConns(1)`：

> `GET_LOCK` 和后续所有 `Exec` **必须落在同一个物理连接上**。不这么做的话，
> `GET_LOCK` 之后的下一条语句可能被连接池分到别的连接，**锁等于没加**。

记账仍走 `config.DB`（GORM，单语句，不需要多语句能力）—— 各用各的长处。

### 6.2 并发安全（不是理论问题）

本地 8080 和 8090 两个实例长期同时开着，`DB_NAME=call_game_dev` 是共用库。
两边同时启动，就会同时看到同一份 pending 清单。

两道保险：

1. `GET_LOCK('call_go:scripts', 10)` 串行化
2. `script_runs.name` 的 `UNIQUE` 索引兜底（真撞上了，第二条 INSERT 会失败，而不是产生重复行）

**流程 6b「拿到锁之后再查一次」是必需的**：如果不重查，实例 B 在 A 执行完之前
就拿到旧快照，等 A 释放锁后 B 照样会把同一个脚本跑第二遍。

### 6.3 checksum 漂移

已成功执行的脚本又被改了 —— 无法判断该不该重跑。

**处理：阻断启动**，错误信息给出两条补救路径：把文件改回去，或者新加一个脚本。

> 待定：这条可能偏严 —— 只改注释也会导致服务起不来。
> 想宽松一点就改成打警告继续跑。

### 6.4 事务

**不包事务。**

MySQL 的 DDL 会隐式提交，包了事务也是假的原子性 ——
一个"看起来有事务保护、实际 DDL 保护不了"的设计比没有更危险。

代价：脚本第 3 句失败时，前 2 句已经落库，且不会记账，下次启动**从头重跑**。

所以这套机制的**承重规则是「脚本必须幂等」** —— 这条要写进
`scripts/sql/README.md` 和 `.env.example` 的注释里。

---

## 七、配置项

按 `config.GetEnv` 的既有约定（`config/config.go:72`）新增：

```bash
# 启动脚本执行模式：off-不执行 / report-只列待执行清单 / apply-真正执行
SCRIPT_RUN_MODE=report

# 磁盘脚本目录覆盖。留空则只用编译进二进制的脚本。
# 开发期设为 scripts/sql，改完脚本重启即可试跑，不用重新编译
SCRIPTS_DIR=
```

---

## 八、非目标（明确不做）

- **shell 脚本**：启动时执行任意 shell = 无审计的代码执行，且换平台就废。不纳入。
- **存储过程 / 触发器**：`DELIMITER` 是 mysql 客户端指令、不是服务端语法，
  走驱动这条路**不生效**。这类需求后续走 Go 注册函数。

---

## 九、改动文件清单

| 文件 | 动作 |
|---|---|
| `models/script_run.go` | 新增，追踪表模型 |
| `scripts/runner.go` | 新增，执行器 |
| `scripts/embed.go` | 新增，`//go:embed sql` |
| `scripts/runner_test.go` | 新增，无 DB 纯函数测试 |
| `scripts/sql/README.md` | 新增，书写约定 + 占位 |
| `config/config.go` | 加 `ScriptRunMode` / `ScriptsDir` 两个字段 |
| `config/database.go` | 抽 `buildDSN`，导出 `DSNWithMultiStatements()` |
| `main.go` | AutoMigrate 加 `&models.ScriptRun{}`；seeds 后调 `scripts.Run` |
| `.env.example` | 补两项配置说明 |
| `go.mod` | `go-sql-driver/mysql` 从 indirect 提为直接依赖 |

**M1 只做 SQL 执行。**

Go 注册函数（`scripts.Register(name, fn)`，共用同一张追踪表、同一个排序命名空间）
留作 M2 扩展点 —— 现在不实现，但执行器的接口设计不会挡住它。

---

## 十、验收方式

### 单元测试

```bash
go build ./...        # CI 只编 ./main.go，编不到包级错误，本地要全量编一次
go test ./scripts/
```

测试只覆盖纯函数，沿用 `utils/hand_test.go` 的风格：包内测试、无 DB、表驱动、
中文子测试名、不用 testify（项目没引断言库）。

覆盖点：文件排序、`.sql` 后缀过滤、pending/drifted 分类、checksum 稳定性。
DB I/O 那部分本地手工验。

### 端到端

> ⚠️ 在**共享 dev 库**上验证，第 4 步之前先备份。

1. `SCRIPT_RUN_MODE=report`，放一个无害脚本，重启
   → 打印待执行清单，`SELECT * FROM script_runs` 为空
2. 改 `apply`，重启 → 执行成功、插一行；**再重启一次** → 报"无待执行脚本"
3. 改动那个已执行的脚本，重启 → 阻断启动，错误信息可操作
4. 分别启动 8080 / 8090 → `script_runs` 只有一行，日志里只有一个实例真的执行了
5. 写个中途报错的脚本 → 服务起不来，错误里能看出是哪个脚本

---

## 十一、待确认项

以下四项当前取的是**最保守**的默认值，实现前可改：

| # | 项 | 当前默认 | 说明 |
|---|---|---|---|
| 1 | 脚本目录 | 新建 `scripts/`，存量 `database/*.sql` 手工执行不动 | 零风险，但两套机制并存 |
| 2 | 默认执行模式 | `SCRIPT_RUN_MODE=report`（默认不执行，只报告） | 共享 dev 库上不会因随手重启被写；代价是开箱不生效。想开箱即用就把默认值改成 `apply` |
| 3 | 失败策略 | 阻断启动（`log.Fatal`） | 半迁移状态的库比服务起不来更危险；逃生通道是 `SCRIPT_RUN_MODE=off` |
| 4 | 分发方式 | `go:embed` 编进二进制 | 现有 CI 不用改；代价是加脚本要重新编译部署 |

第 2 条和第 4 条最影响日常手感。
